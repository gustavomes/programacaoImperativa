//aula 16
//Callback

let minhaCallback = () => console.log("Olá turma!");



let fizzBuzz = () => {
    for (let i = 0; i <= 100; i++) {
        if ((i % a === 0) && (i % b === 0))
            console.log('FizzBuzz')
        else if (i % a === 0)
            console.log('Fizz')
        else if (i % b === 0)
            console.log('Buzz')
        else
        console.log(i)
      }  
    }

    fizzBuzz(3,5)


    setTimeout(minhaCallback, 1000);
setTimeout(minhaCallback, 2000);
setTimeout(minhaCallback, 3000);
setTimeout(minhaCallback, 4000);
